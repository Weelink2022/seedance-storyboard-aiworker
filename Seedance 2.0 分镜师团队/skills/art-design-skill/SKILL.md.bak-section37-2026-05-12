---
name: art-design-skill
description: 服化道技能。用于设计人物设定提示词和场景环境提示词。
---

# 服化道设计技能

[技能说明]
    美术设计技能，将导演提供的人物清单、音色清单和场景清单转化为可直接用于文生图工具生成参考图的详细提示词，并把音色垫词原样搬运到 character-prompts.md。

    人物提示词出图格式：一张图，左半边面部特写，右半边全身正面/侧面/背面设定图，白色背景。
    场景提示词出图格式：一张宫格图，每格一个场景。
        - ≤9 个场景 → 3×3 九宫格
        - 10-12 个场景 → 3×4 宫格
        - 13-16 个场景 → 4×4 宫格
        - 不满格数的空格留空。

    音色垫词处理（§36）：导演已为每个人物写好 60-100 字音色垫词，服化道**原样照抄**到 character-prompts.md 的音色垫词副段，不改写不扩写。分镜师下游会原样拼到台词前。

[文件结构]
    art-design-skill/
    ├── SKILL.md                                    # 本文件（技能流程说明）
    ├── gemini-image-prompt-guide.md                # Gemini 提示词指南（叙事式写法）
    ├── examples/                                   # 高质量提示词示例
    │   ├── character-prompt-examples.md            # 人物设定提示词示例
    │   └── scene-prompt-examples.md                # 场景环境提示词示例
    └── templates/
        └── art-design-template.md                  # 服化道设计产出模板（输出格式定义）

[执行流程]

    第一步：读取上游产物
        - 读取导演分析产出中的视觉风格说明、人物清单和场景清单
        - 读取 gemini-image-prompt-guide.md 了解提示词写法规范
        - 读取 examples/ 下的示例文件，了解高质量提示词的写法标准
        - 读取已有的 assets/character-prompts.md 和 assets/scene-prompts.md（如存在），了解已设计的人物和场景
        - 根据人物清单和场景清单中的"素材状态"列，只为标注为"新增"或"变体"的项设计提示词
        - "复用"项跳过，不需要设计

    第二步：设计人物造型提示词
        为每个人物编写设定提示词。**每个人物必须包含基础形象 + 按场景/状态的变体形象**。

        出图布局（基础形象）：
        - 一张图，左半边面部特写，右半边全身正面/侧面/背面三视图设定图
        - 白色干净背景
        - 角色设定图风格

        基础形象提示词必须包含：
        - 角色整体定位（年龄、性别、国籍/民族特征）
        - 面部细节：脸型、五官、肤色、发型/发色、表情
        - 体型：身高、体态
        - 服装：款式、颜色、材质、细节
        - 配饰：手表、耳环、眼镜、包等
        - 鞋子
        - 整体气质

        美学质量要求：
        - 提示词中须融入美学导向描述，提升人设图的品质感
        - 在叙事描述中自然加入：电影级光影、皮肤质感真实、五官立体精致等
        - 保持叙事描述式写法，不要关键词堆叠

        变体形象体系（新增）：
        同一人物在不同场景和状态下需有变体提示词。导演人物清单中标注"变体"的项须设计。

        变体类型一：场景着装变体
        - 根据导演场景清单中的"着装要求"列，为同一人物在不同场景设计不同着装
        - 每个变体独立成段，标注适用场景
        - 示例：林薇（基础：白领职业装）→ 变体1：居家睡衣（家庭场景）→ 变体2：运动服（体育场场景）

        变体类型二：状态变化变体
        - 角色遭遇受伤、湿身、衣物撕裂、脏污、发型散乱等变化时
        - 为每种变化状态设计变体提示词，标注触发条件
        - 变体只在基础形象上做定向修改，面部/体型等不变
        - 示例：林薇（基础）→ 变体：右脸颊血痕、外套撕裂、头发散落（战斗后）
        - 描述方式："[基础形象描述]，但此刻她的右脸颊有一道血痕..."

        变体命名规范：[人物名]_变体_[场景/状态名]
        在 character-prompts.md 中按"基础 → 变体1 → 变体2 → ..."顺序排列

        **音色垫词**（§36 新增，原样搬运不改写）：
        - 从导演分析产出的「## 音色清单」中找到对应人物，把 `音色垫词` 列的整段叙事描述**原样照抄**到该人物的 character-prompts.md 段落里，写在视觉提示词之后、以独立的 `**音色垫词**:` 副段呈现。
        - **不要改写、不要扩写、不要润色**——音色描述是导演定型，分镜师下游会原样使用。
        - 即使你认为措辞可以更精炼或更生动，也保持原文。改动会破坏分镜的一致性。
        - 如果导演音色清单里某人物缺失，回报错误，不要自己编造音色描述。
        - 若同一人物在不同集出现「嗓音变体」（导演音色清单中标"变体"），将变体音色垫词作为该人物变体体系的一部分写入（与外观变体并列）。

    第三步：设计场景环境提示词
        每个场景以多宫格格式生成，展示该场景的不同视角。

        每个场景独立出一张宫格图，展示该场景从不同角度/焦距的视觉状态：

        宫格规格（单个场景）：
        - 普通场景：2×2 四宫格（1个全景建立 + 3个不同角度/焦距的画面）
        - 重要/复杂场景：3×3 九宫格（多角度全覆盖）
        - 每个格子 = 同一场景的不同视角

        提示词每个格子必须包含：
        - 地点类型与空间布局
        - 时间（影响光照）
        - 光源类型与光线方向
        - 整体色调/色彩倾向（须与导演分析中的视觉风格说明一致）
        - 关键道具/家具/物件
        - 空间纵深与层次
        - 氛围/情绪
        - 风格（写实/电影感等，须与导演分析中的视觉风格说明一致）
        - **视角说明**（如：全景建立 / 中景 / 特写 / 高角度俯拍 / 低角度仰拍等）

        所有格子必须保持视觉风格统一，光影逻辑自洽。
        不同场景之间的宫格图颜色/风格可以不同（因为场景本身氛围不同）。

    第四步：输出
        按照 templates/art-design-template.md 定义的格式，分别写入两个文件：
        - 人物提示词 → 追加到 assets/character-prompts.md
        - 场景道具提示词 → 追加到 assets/scene-prompts.md
        首集时创建文件，后续集数追加新增/变体条目到文件末尾。

[提示词写法原则]
    参考 gemini-image-prompt-guide.md，核心要点：

    - 叙事描述式：用完整段落描述，不要关键词堆叠
    - 具体化：所有描述必须具体到可视化的程度
        - "穿红色衣服" ✗ → "穿深红色修身棉袄外套，内搭碎花棉布衫" ✓
        - "室内场景" ✗ → "现代都市写字楼的开放式办公区，大部分工位已空" ✓
    - 摄影术语融入自然语言
    - 情绪通过视觉细节传达，不要抽象描述

[注意事项]
    - gemini-image-prompt-guide.md 是提示词写法的核心参考
    - 人物提示词必须明确"左半边面部特写 + 右半边三视图"的布局要求
    - 场景提示词要包含足够的环境细节以确保场景可信度和一致性
    - 如果是为变体设计（如同一人物的冬装版），需在提示词中注明与原版的关联和差异
