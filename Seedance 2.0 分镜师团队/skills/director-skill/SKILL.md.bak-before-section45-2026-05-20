---
name: director-skill
description: 导演执行技能。用于剧本分析、剧情拆解、导演讲戏。导演在阶段一使用此 skill 将剧本转化为导演讲戏本、人物清单和场景清单。
---

# 导演执行技能

[技能说明]
    剧本分析与导演讲戏技能。将剧本/梗概/分场大纲转化为：
    1. 导演讲戏本（按剧情点分节，每节包含元数据 + 导演阐述）
    2. 人物清单（用于服化道生成角色参考图）
    3. 场景清单（用于服化道生成场景参考图）

    核心理念：导演像给演员讲戏一样，把脑海中的画面完整描述出来。
    一个剧情点 = 一段完整叙事 = 一次 Seedance 2.0 生成任务。
    段落内可包含多组镜头（如蒙太奇、快速闪回），但叙事上必须是一个完整段落。

[输入来源（§39 流程调整，2026-05-17）]
    本技能除剧本原文外，还会收到一份由 smart_analysis 预先产出的
    **结构化分镜表**（前期镜头规划蓝图：字段化的镜号/景别/摄法/时长/
    画面/台词/角色/场景）。两者配合使用，分工原则：
    - 清单类信息（场景清单、人物名单、道具名单）—— **优先参考结构化
      分镜表**，省去从零重新识别；分镜表的「场景标识」「入镜角色」列
      可直接取用。
    - 理解/权重/音色类信息（人物设定细节、道具叙事权重与变体触发点、
      音色垫词）—— **仍以剧本原文为准**。结构化分镜表为出画面只保留
      「画面事实」，不含叙事潜台词、人物整体性、音色依据，这些只在
      剧本里，必须由导演通读剧本得出。
    - 结构化分镜表是参考蓝图，不是命令：剧情点划分、时长可在其基础上
      按叙事需要调整（Seedance 单条 4-15 秒上限仍是硬约束）。

[文件结构]
    director-skill/
    ├── SKILL.md                                    # 本文件（技能流程说明）
    └── templates/
        └── director-analysis-template.md           # 导演分析产出模板（输出格式定义）

[执行流程]

    第一步：通读剧本
        - 读取用户提供的剧本/梗概/分场文本
        - 读取 smart_analysis 产出的结构化分镜表（§39，若有）
        - 理解核心叙事线、人物关系、情感走向
        - 识别关键转折点和高潮
        - 注意：理解叙事/人物/情感必须基于剧本原文；结构化分镜表仅作
          清单核对与剧情点划分的参考

    第二步：基于分镜提炼剧情点（§39 改造，2026-05-17）

        【新工作方式】不再从剧本自由拆剧情点，而是以 smart_analysis 产出的
        结构化分镜表为基础，把有序的细镜头提炼归并成剧情点 P。

        情形 A —— 收到 smart_analysis 结构化分镜表（正常情形）：
        1. 结构化分镜表是一串有序细镜头：镜1、镜2、镜3 …（按剧情顺序）。
        2. 按相邻顺序归并：从镜1 起，把连续的、服务于同一叙事段落的若干
           细镜头并成一个剧情点 P。指针只往后走、不回头。
           - 相邻细镜头属同一叙事段落（同场景内连续动作、同一叙事目的）
             → 并入同一个 P；
           - 遇叙事自然断点（情绪转折 / 时空跳转 / 叙事功能转换）→ 收束
             当前 P，从下一个细镜头开新的 P。
        3. 一个 P 通常归并 1-4 个相邻细镜头。
        4. 因为按顺序、指针不回头地归并，每个细镜头有且只属于一个 P，
           全部细镜头被连续覆盖 —— 天然不重不漏。
        5. 注意：同一场景代码下的细镜头若叙事上分属两段（如"公寓记录数据"
           与"公园晨跑"虽同场景，是两个叙事段落），应拆成两个相邻 P，
           各归并自己那段的镜号，不得让某镜号同时进两个 P。

        【强制】每个剧情点 P0X 元数据里必须标一行：
           - 包含分镜：镜a,镜b,镜c
        列出本 P 归并的细镜头镜号（连续区间，逗号分隔）。
        自检：所有「包含分镜」行的镜号合并 = 完整镜号序列（镜1…镜N），
        无重复、无遗漏、无交叉。

        情形 B —— 无结构化分镜表：
        回退到从剧本自身拆剧情点（情绪转折 / 时空跳转 / 叙事功能转换为
        断点），可省略「包含分镜」行。

        一个剧情点内可包含多组镜头（蒙太奇序列 / 连续场景 / 单镜头）。

        说明：本步只决定"P 怎么划分、各含哪些镜号"。P 的导演阐述内容在
        第四步写，依据是剧本原文（结合本 P 归并的细镜头作画面参考，但
        阐述文字自由创作，不照抄细镜头的描述措辞）。

    第三步：确定时长

        Seedance 2.0 单次生成 4-15 秒。根据段落的叙事密度选择时长。

        时长选择参考：
            | 时长 | 适用场景                                         |
            |------|--------------------------------------------------|
            | 5s   | 单镜头，一个动作或表情变化                       |
            | 8s   | 单镜头，一组连贯动作                             |
            | 10s  | 多镜头，一段包含镜头转换的叙事                   |
            | 15s  | 多镜头，完整的多场景叙事（如蒙太奇、连续切换）   |

        节拍密度约束（作用于每个连续镜头内，非整段）：
            底层规律：AI 视频生成中，每个连续镜头内 1 拍 ≈ 2.5 秒屏幕时间。节拍太密，动作会糊、跳、不自然。

            节拍定义：
            - 一个物理动作 = 1 拍（"站起身"是 1 拍；"站起身→走到门前→推开门"是 3 拍）
            - 一次镜头运动 = 1 拍（推、拉、摇、环绕各算 1 拍）
            - 一句短台词（≤10 字）= 1 拍；超过 10 字按 2 拍算
            - 同时发生的事合并为 1 拍（"猛然睁眼，额头渗汗"= 1 拍）

            多镜头段落的节拍分配：
            段落内的节拍分布在多个镜头切换中，每个镜头各自遵守密度约束。
            例：15 秒蒙太奇可含 5 个子画面，每个 2-3 秒、1-2 拍，总节拍远超单镜头上限。

        头尾安全区：
            每次 Seedance 生成的前 0.5 秒和后 0.5 秒为安全区，不放关键内容。
            开头用于场景建立，结尾用于收住或过渡。

    新增步骤：确定视觉风格（在讲戏之前完成）
        在输出文档头部编写视觉风格说明，作为该集全局视觉参数：
        - 整体风格（真人写实 / 3D CG / 国漫 / 韩漫 / 日漫 / 自定义描述）
        - 色彩基调（暖/冷/高饱和/低饱和等）
        - 光影方向（硬光/柔光/自然光/戏剧性布光）
        - 材质质感（胶片颗粒/光滑/粗糙/水墨等）
        - 画幅比例（16:9 / 9:16 / 21:9 等）

        此全局参数将传递给服化道和分镜师，确保全阶段视觉一致。

    第四步：为每个剧情点"讲戏"
        剧情点 P 的划分与各自含哪些镜号已在第二步基于分镜定好，本步只为
        每个已定的 P 写导演阐述（结合剧本原文 + 本 P 归并的细镜头作画面参考）。
        先规划镜头 → 再写导演阐述。

        镜头规划（在写导演阐述前先完成）：
        每个剧情点须规划该段的镜头演进链，分三列：
        - 景别演进：如"全景建立 → 中景对话 → 面部特写"
        - 角度变化：如"平视 → 过肩 → 近景"
        - 运镜变化：如"固定 → 缓推 → 固定"
        多镜头段落分行列出每组镜头的规划。

        镜头数（紧跟镜头规划之后，必须输出）：
        每个剧情点 P 的元数据里必须有一行「- 镜头数：N」。
        N = 该 P「景别演进」链里的镜头个数（即景别那一条 → 链有几段就是几个；
        只数景别链，角度/运镜行不计入）。例：景别演进"全景 → 中景 → 特写"则 N=3。
        这个 N 是下游分镜师拆镜的硬依据——分镜师会按此数把该 P 严格拆成 N 个细镜头，
        不多不少。因此 N 必须与景别演进链严格一致，不得随意填写。

        然后再写导演阐述，阐述必须包含以下维度（不需要分条列举，用自然流畅的叙述融合在一起）：

        画面内容：谁在哪里、环境是什么样
        人物动作：具体的肢体动作、走位、表情变化
        台词/声音：说了什么话、什么语气、环境音
        镜头感觉：什么景别、镜头怎么运动、从哪到哪
        光影氛围：什么光源、什么色调、什么情绪

        讲戏原则：
        - 用具体的物理动作，不用抽象概念（"揉太阳穴" ✓，"表现疲惫" ✗）
        - 用连续的动作链，让画面"动起来"
        - 镜头运动要有明确方向（"从侧脸推向窗外" ✓，"镜头移动" ✗）
        - 光影描述要具体（"冷白荧光灯照在脸上" ✓，"灯光" ✗）
        - 情绪通过动作和环境传达，不要直接说"她很伤心"
        - 微表情设计（仅近景/特写镜头）：
            - 近景和特写镜头中，将微表情作为"微观动作链"来描述——有运动过程、有时间推移、有变化轨迹
            - 格式：触发反应 → 变化过程 → 最终状态，且与镜头运动/环境元素协同
            - 静态标签写法（不要用）："瞳孔微缩""眼角泛红""表情坚定"
            - 动态过程写法（正确示范）："呼吸突然屏住，瞳孔先是茫然地散开，随后急剧收缩聚焦，像猎人锁定了猎物"
            - 非近景/特写镜头不需要微表情描述
        - 无旁白（短剧红线）：短剧中**禁止使用旁白/画外音叙事**。所有叙事信息须通过台词、动作、画面元素和音效传达。角色内心独白应以角色开口说话（有口型动作）的方式呈现，而非以第三方叙述性"旁白"的形式。
        - AI 视频生成限制（现代都市/写实题材）：
            - 避免描述物理破坏场景（踹门木屑飞溅、砸碎显示器、玻璃碎裂、物品爆裂等）
            - AI 视频模型处理物理破坏类场景容易出现穿帮/闪烁/变形
            - 需要表现激烈冲突时，改用人物表情/动作/运镜/音效来传达
            - "门被猛然踹开，木屑飞溅" ✗ → "门被猛然推开，重重撞在墙上，发出一声巨响" ✓
            - 古装/奇幻/动作题材中的武打破坏可适当放宽（但需评估AI生成效果）
        - 避免漫画/动漫视觉符号（写实题材）：
            - "眼镜闪过一道冷光""镜片反光闪白"这类描述是动漫视觉语法（柯南式），AI 会将其理解为漫画特效帧，在写实画风中生成闪光帧，导致画风突变
            - 改用真实布光中的物理光学现象来替代
            - "银边眼镜闪过一道冷光" ✗ → "镜框边缘反射着天花板的暖色筒灯光点，随着他转头的动作，光点从镜框前端滑向后端" ✓

        多镜头段落的额外要求：
        当一个剧情点包含多组镜头时，还需描述：
        - 镜头之间的切换方式（硬切、叠化、闪白、画面碎裂等）
        - 子画面之间的节奏（越来越快、均匀、先慢后快等）
        - 整段的叙事弧线（如"从希望到绝望的递进"）

    第五步：提取人物清单
        从剧情中筛选需要生成参考图的人物，只记录视觉外观信息：
        - 人物名
        - 年龄
        - 外观关键词（五官、发型、体型、服装等）

        筛选标准（满足任一即列入清单）：
        - 本集出现在 2 个及以上剧情点中的人物
        - 虽然本集只出现 1 次，但根据已有剧本判断后续集数会再次出场的人物

        不列入清单的人物（群演/一次性配角）：
        - 本集只出现 1 次且不会再出场的角色（如路人、围观群众、背景人物）
        - 这些人物不进清单，但在讲戏本的导演阐述中照常描述其外观和动作
        - 描述要足够具体，让分镜师能直接在提示词中用文字描写（如"尖脸眯缝眼的白袍学员"而非"一个路人"）

        注意：
        - 不需要情绪弧线，参考图是静态角色设定图
        - 人物清单 = 需要参考图的角色列表，服化道会为清单中的每个角色设计提示词

    第六步：提取音色清单（§36 新增）
        为人物清单中**每一个**角色编写一段「音色垫词」——一段约 60-100 字的中文叙事描述，作为角色嗓音/语气的定型，下游分镜师在该角色每句台词前都会原样内联此段。

        编写依据（综合下列要素推断）：
        - 角色性别 / 年龄段 / 性格定位（来自人物清单 + 剧本上下文）
        - 角色在剧情中的对白语气、情绪基调
        - 与剧本风格匹配的音色调性（写实/古装/玄幻/职场等）

        §39 注意：音色垫词**必须基于剧本原文**对角色的理解来写。
        smart_analysis 结构化分镜表里没有音色信息，也推不出——不要
        从分镜表提取音色，分镜表只能帮你确认"有哪些角色"。

        必含维度（融合成一整段叙事，不分条）：
        - 性别 + 年龄段（如"男声，青壮年音色" / "女声，少女音色"）
        - 音调（低 / 中等偏低 / 中等 / 中等偏高 / 高）
        - 音色质感（沉稳 / 磁性 / 清亮 / 冷冽 / 温婉 / 沙哑 / 清脆 等）
        - 声线特质（清冽带哑 / 浑厚干净 / 柔和通透 / 明亮清脆 等）
        - 发音方式（利落克制 / 干净标准 / 含混 等）
        - 气息（沉敛绵长 / 平稳深沉 / 充沛平稳 / 急促 等）
        - 吐字与语速（锋利清晰 / 偏慢 / 中等 / 偏快 等）
        - 言语风格 / 情绪基调（"不容置疑的掌控力" / "与生俱来的温婉与真诚感" / "疏离阴鸷与漫不经心的压迫感" 等）

        风格示例（直接照此粒度写）：

            男主：男声，青壮年音色，音调低，音色质感沉稳且带有磁性，声音浑厚，发音
            干净，气息平稳深沉，吐字清晰、语速偏慢，话语间有种不容置疑的掌控力。

            男反派 Arthur：男声，青年音色，音调中等偏低，音色质感冷冽、低沉，声线清冽
            带哑，发音方式利落克制，气息沉敛绵长，吐字锋利清晰，自带一种疏离阴鸷与漫
            不经心的压迫感。

            女主 Karya：女声，青年音色，音调中等偏高，音色质感明亮、清脆，声音清亮柔
            和，发音方式干净，气息充沛平稳，吐字清晰，带有一种与生俱来的温婉与真诚感。

            女反派 Mia：年轻女性声音，18 岁少女，音色清亮柔和，声音干净通透，气息平稳
            自然，语速中等偏慢，吐字清晰，发音标准，语气温柔但带轻微控制感，情绪克制
            内敛，带若有若无的微笑感，整体风格温婉却隐藏危险感。

        筛选规则：
        - 人物清单里**所有列入的角色**都必须有音色垫词（与人物清单一一对应）
        - 群演/一次性配角不在人物清单中，所以也无须为他们写音色垫词
        - 同一人物若在不同集出现"嗓音变化"（变声、老化、受伤后声音嘶哑等），ep0X
          复测时把此变化在音色清单中标"变体"并写新垫词；art-designer 会作为变体复制

    第七步：提取道具清单（§37 新增）
        从剧情中筛选需要参考图的道具，只记录视觉外观信息。道具是 Character/Scene 之外的第三种实体——指被人拿在手里 / 放在场景里 / 独立特写镜头出现的关键物品（手机、武器、医疗器械、证件、信件、品牌商品等）。

        筛选标准（满足任一即列入清单）：
        - 本集出现在 2 个及以上剧情点中的道具（包括"被人持"和"独立特写"两种形态合计）
        - 虽然本集只出现 1 次，但根据已有剧本判断后续集数会再次出场的道具
        - 在剧情中起到叙事关键作用的道具（如"母亲的旧手机引发回忆""那封信改变了人物决定"）

        不列入清单的道具（一次性背景物）：
        - 街景里的随意物件、餐桌上的普通餐具、办公桌上一般文件等氛围道具
        - 这些不进清单，但在讲戏本的导演阐述中照常描述

        每个道具记录的字段：
        - 道具名（中文短词，如"林远的智能手机""父亲的金边眼镜"）
        - 类型（电子产品/武器/服饰配饰/文件单据/医疗器械/餐具炊具/家具/交通工具/其他）
        - 外观关键词（材质、颜色、形状、尺寸、状态、品牌/无品牌、特殊标记）
        - 素材状态（新增 / 复用 / 变体）

        变体（重要）：
        - 同一道具如果在剧情中发生状态变化（手机裂屏 / 衣物被血染 / 器械损坏 / 信件被撕碎），
          需在道具清单中标注"变体"，服化道会为变体单独设计一张参考图。
        - 状态变化的触发剧情点要明确标注，便于分镜师在该剧情点改用变体引用。

    第八步：提取场景清单
        从剧情中提取所有关键场景，只记录视觉环境信息：
        - 场景名
        - 时间（白天/夜晚/傍晚等）
        - 光线/色调
        - 氛围关键词
        - **角色着装要求**：该场景中各主要角色的着装（如"林薇：职业西装 / 母亲：家居服"），此列为变体提示词设计的源头输入

    第九步：素材比对（ep02+ 时执行）
        如果 assets/character-prompts.md / assets/scene-prompts.md / assets/prop-prompts.md 存在，读取已有素材，为本集人物清单/音色清单/道具清单/场景清单中的每一项标注素材状态：

        判断规则：
        - 同一人物/场景/音色/道具未变化 → 标注"复用"
        - 新人物/新场景/新音色/新道具 → 标注"新增"
        - 同一人物外观变化（换装、老化、受伤等）→ 标注"变体"
        - 同一人物嗓音变化（变声、严重沙哑、特殊场合的耳语等）→ 音色清单里标"变体"
        - 同一道具状态变化（损坏、染色、撕碎等）→ 道具清单里标"变体"
        - 同一场景状态变化（季节、时间、破坏等）→ 标注"变体"

        ep01 或提示词文件不存在时，所有项标注"新增"。

    第十步：输出
        按照 templates/director-analysis-template.md 定义的格式输出。
        人物清单/音色清单/道具清单/场景清单中须包含"素材状态"列，服化道据此判断哪些需要设计。

[质量标准]
    - 每个剧情点的导演阐述至少 3-5 句话，有足够的视觉信息
    - 讲戏内容必须让分镜师能直接提取出运镜、动作、台词、光影
    - 剧情点之间要有叙事推进，不能重复信息
    - 人物清单和场景清单的外观关键词要足够具体，服化道能据此设计提示词
    - **音色清单与人物清单严格一一对应**：清单里每个角色都必须有音色垫词，长度约 60-100 字
    - **道具清单**：含叙事关键作用或多次出场的道具,外观关键词要具体到品牌/材质/状态级,§37 新增
